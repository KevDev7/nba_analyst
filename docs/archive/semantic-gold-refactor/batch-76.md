# Batch 76

Added `two_pointers_made_total` and `two_pointers_attempted_total` to `semantic_gold.player_season`.

## Why

These season totals were already being accumulated internally to support `two_pointers_percentage`.

This batch simply exposes them as public season totals.

## Added

- `two_pointers_made_total`
- `two_pointers_attempted_total`
